/**
 * TYPES
 */

// @todo Add more properties as needed for Member Profile features
export type Member = {
  name: string;
  jobTitle: string;
  address: string;
  delegateKey: string;
  isDelegated: boolean;
  units: string;
};
